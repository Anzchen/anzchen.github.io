# Logs
logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
pnpm-debug.log*
lerna-debug.log*

node_modules
dist
dist-ssr
*.local

# Claude Code local files
.claude/skills/
!.claude/settings.local.json

# Editor directories and files
.vscode/*
!.vscode/extensions.json
.idea
.DS_Store
*.suo
*.ntvs*
*.njsproj
*.sln
*.sw?

# Merged AI-expanded painting sources (33MB). Build input for
# scripts/make-painting-layers.mjs, not needed to build the site: the shipped
# layers in src/assets/painting/ are committed. Originals and the AI outputs
# they were merged from live in ~/Documents/painting-cuts/.
src/assets/painting-src/

# Retired ink-plate art, kept only so a leftover local copy is not committed.
# The plates were dropped from the design and their build script is gone.
src/assets/plates-src/
